# Explore the legacy application
*This application doesn't follows all of the 12 factors*

# Run the legacy app

*Open a Terminal in VScode*

```bash
dotnet run # Starts the application in development mode
```